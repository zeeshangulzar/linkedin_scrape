<%= form_with(model: daraz) do |form| %>
  <% if daraz.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(daraz.errors.count, "error") %> prohibited this daraz from being saved:</h2>

      <ul>
        <% daraz.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title, required: true %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price, required: true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
